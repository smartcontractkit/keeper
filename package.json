{
  "name": "chainlink-contract-callers",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:kovan": "hardhat deploy --network kovan --gasprice 1000000000",
    "verify:kovan": "hardhat etherscan-verify --network kovan --api-key $ETHERSCAN_API_KEY",
    "deploy:mumbai": "hardhat deploy --network mumbai --gasprice 1000000000",
    "verify:mumbai": "hardhat etherscan-verify --network mumbai --api-key $ETHERSCAN_API_KEY"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.1.6",
    "@nomiclabs/hardhat-truffle5": "^2.0.0",
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "hardhat": "^2.3.0",
    "hardhat-gas-reporter": "^1.0.4",
    "truffle": "^5"
  },
  "devDependencies": {
    "@openzeppelin/test-helpers": "^0.5.9",
    "hardhat-deploy": "^0.7.0-beta.44"
  }
}
